#include <iostream>
#include <string>
#include <vector>

std::string disemvowel(const std::string& str) {
  std::vector<char> garbage;
  std::vector<char> result;
  const std::string vowels = "aeiouw";
  for (int i : str) {
    if (
      result.push_back(i);
    }
  }
  for (char i : result) {
    std::cout << i << std::endl;
  }
  return "hello world";
}
int main() {
  std::cout << disemvowel("This website is for losers LOL!") << std::endl;
}
